{"id":"health-clamp","set":"train","tags":["logic","bounds"],"goal":"A Health component with current and max HP. TakeDamage(int) and Heal(int) must never let HP go below 0 or above max, and negative arguments must not heal or damage in reverse."}
{"id":"cooldown-timer","set":"train","tags":["logic","frames"],"goal":"An ability cooldown component. TryUse() succeeds only when ready and starts the cooldown; Tick(float deltaTime) advances it; IsReady becomes true exactly when the cooldown elapses."}
{"id":"stamina-regen","set":"train","tags":["logic","frames","bounds"],"goal":"A Stamina component that drains on Use(int) and regenerates over time via Tick(float deltaTime) at a configurable rate, clamped to 0..Max, with a delay before regeneration starts after use."}
{"id":"move-to-target","set":"train","tags":["frames"],"goal":"A component that moves its transform toward a target position at a configurable speed, arriving exactly at the target without overshooting, and reporting when it has arrived."}
{"id":"score-tracker","set":"train","tags":["perf","collections"],"goal":"A ScoreTracker component that records scores via Record(int), which may be called every frame, and exposes Total and the highest score. It must not allocate on the hot path."}
{"id":"object-pool","set":"train","tags":["perf","collections"],"goal":"A GameObject pool component with Get() and Release(GameObject). Reuse instances instead of instantiating, grow on demand, and do not allocate per Get/Release call."}
{"id":"respawn-timer","set":"train","tags":["logic","frames"],"goal":"A respawn component. When Kill() is called the entity is dead; Tick(float deltaTime) counts down a configurable delay and revives it exactly once when the delay elapses."}
{"id":"damage-over-time","set":"train","tags":["logic","frames"],"goal":"A damage-over-time effect. Apply(int damagePerSecond, float duration) then Tick(float deltaTime) deals damage in whole points as time accumulates, ending exactly at the duration without over-applying."}
{"id":"buff-stack","set":"train","tags":["logic","collections"],"goal":"A buff container that holds stacking buffs, each with a magnitude and a remaining duration. Tick(float deltaTime) expires them, and TotalMagnitude reflects only the active ones."}
{"id":"camera-follow","set":"train","tags":["frames"],"goal":"A camera follow component that moves toward a target with configurable smoothing and an offset, using Tick(float deltaTime) so the behavior can be verified without waiting for real frames."}
{"id":"grid-spawner","set":"train","tags":["render","perf"],"goal":"A component whose Build() spawns a square grid of floor tiles with a configurable size and spacing. Keep the draw call cost low for a large grid."}
{"id":"jump-input","set":"train","tags":["input","frames"],"goal":"A jump component driven by the Space key through the Input System. Pressing Space starts a jump; the entity lands automatically after a configurable duration and cannot jump again while airborne."}
{"id":"inventory-stack","set":"holdout","tags":["logic","collections","bounds"],"goal":"An inventory that holds stacks of items by id with a maximum stack size and a maximum number of slots. TryAdd returns how many items did not fit, and TryRemove never removes more than is held."}
{"id":"state-machine","set":"holdout","tags":["logic"],"goal":"A three-state machine (Idle, Moving, Attacking) with explicit allowed transitions. Illegal transitions are rejected without changing state, and entering a state raises an event exactly once."}
{"id":"wave-spawner","set":"holdout","tags":["frames","render"],"goal":"A wave spawner that spawns a configurable number of enemies per wave with a delay between waves, advanced by Tick(float deltaTime), stopping after the last wave."}
{"id":"nearest-target","set":"holdout","tags":["perf","collections"],"goal":"A component that finds the nearest target within a radius from a list it is given. The selection runs every frame, so it must not allocate and must avoid unnecessary square roots."}
{"id":"projectile-pool","set":"holdout","tags":["perf","render","collections"],"goal":"A projectile launcher that fires pooled projectiles at a configurable rate via Tick(float deltaTime), returning them to the pool after their lifetime expires, without allocating per shot."}
{"id":"move-input","set":"holdout","tags":["input","frames"],"goal":"A movement component driven by WASD through the Input System. Holding a key moves the transform at a configurable speed; diagonal movement must not be faster than straight movement."}
